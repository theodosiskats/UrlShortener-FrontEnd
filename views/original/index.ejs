<%- include('../partials/header') %>

<form action="original" method="POST" class="validated-form" enctype="application/x-www-form-urlencoded">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <p class="display-5">Shorten URL</p>
                <label for="url" class="form-label">Enter the url you want to shorten</label>
                <input type="text" name="url" id="url" class="form-control" aria-labelledby="urlHelpBlock">
                <div id="urlHelpBlock" class="form-text">
                    This particular example doesn't test if the text entered is an url or not, therefore it will respond with a shortened version of whatever you enter.
                </div>
                <div class="mt-3">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </div>
        </div>
    </div>
</form>

<%- include('../partials/footer') %>
